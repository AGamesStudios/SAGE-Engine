name: CI
on: [push, pull_request]

jobs:
  minimal:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      - run: python bootstrap.py
      - run: ruff check .
      - run: pytest -q

  qt6:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      - run: python -m pip install .[dev,qt6]
      - run: ruff check .
      - run: pytest -q

  tk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      - run: python -m pip install .[dev,tk]
      - run: ruff check .
      - run: pytest tests/gui/test_auto_load.py -q
