# 📘 Таймеры

🧠 Модуль `timers` позволяет выполнять колбэки через заданный интервал. Таймеры не выделяют память в игровом цикле и имеют лимит `MAX_PER_FRAME` на обработку. Пропущенные таймеры учитываются в `profiling.profile.timers_dropped`.
