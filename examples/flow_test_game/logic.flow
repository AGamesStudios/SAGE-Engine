переменная player_x = 320

при обновление сделай
    global player_x
    если Input.is_pressed("LEFT") тогда
        player_x -= 5
    если Input.is_pressed("RIGHT") тогда
        player_x += 5
конец
