#pragma once

#include "KeyCodes.h"

namespace SAGE {

// Триггеры геймпада (отдельный enum для более удобного API)
enum class GamepadTrigger {
    Left = 0,       // Левый триггер (L2/LT)
    Right = 1,      // Правый триггер (R2/RT)
};

} // namespace SAGE
