# 🎮 SAGE Engine - Примеры игр

Полноценные примеры 2D игр, демонстрирующие возможности движка.

---

## 📚 Доступные примеры

### 1. 🚀 Space Shooter (Космический шутер)
**Файл:** `SpaceShooter.cpp`

**Описание:**
Классический вертикальный космический шутер с волнами врагов.

**Возможности:**
- ✅ Управление космическим кораблем (WASD)
- ✅ Стрельба лазерами (SPACE)
- ✅ Система волн врагов (увеличивающаяся сложность)
- ✅ Вражеские пули
- ✅ Подсчет очков
- ✅ Система здоровья
- ✅ Эффекты частиц (взрывы, звезды)
- ✅ Оптимизация коллизий через QuadTree
- ✅ Возможность перезапуска (R)

**Управление:**
- `WASD` - Движение корабля
- `SPACE` - Стрельба
- `R` - Перезапуск после Game Over

**Компиляция:**
```bash
# Добавить в CMakeLists.txt:
add_executable(SpaceShooter Examples/SpaceShooter.cpp)
target_link_libraries(SpaceShooter SAGE_Engine)

# Собрать:
cmake --build build --config Release --target SpaceShooter

# Запустить:
.\build\bin\Release\SpaceShooter.exe
```

**Изученные техники:**
- Система волн (wave-based gameplay)
- Пулевой паттерн
- QuadTree для оптимизации
- Particle effects
- Game Over и Restart система

---

### 2. 🐦 Flappy Bird
**Файл:** `FlappyBird.cpp`

**Описание:**
Клон знаменитой мобильной игры с процедурной генерацией препятствий.

**Возможности:**
- ✅ Физика гравитации
- ✅ Управление прыжком (SPACE)
- ✅ Процедурная генерация труб
- ✅ Система подсчета очков
- ✅ Коллизии с препятствиями
- ✅ Анимация вращения птицы
- ✅ Фоновые облака (частицы)
- ✅ Простой UI

**Управление:**
- `SPACE` - Прыжок / Старт игры
- `R` - Перезапуск

**Компиляция:**
```bash
add_executable(FlappyBird Examples/FlappyBird.cpp)
target_link_libraries(FlappyBird SAGE_Engine)

cmake --build build --config Release --target FlappyBird
.\build\bin\Release\FlappyBird.exe
```

**Изученные техники:**
- Физика (гравитация, скорость)
- Процедурная генерация уровней
- Endless runner паттерн
- Простая система прокрутки
- Game state management

---

### 3. 🧱 Breakout (Arkanoid)
**Файл:** `BreakoutGame.cpp`

**Описание:**
Классическая аркада с разрушением блоков.

**Возможности:**
- ✅ Управление ракеткой (клавиатура + мышь)
- ✅ Физика отскока мяча
- ✅ Несколько типов блоков (обычный, прочный, неразрушимый)
- ✅ Система уровней
- ✅ Жизни игрока
- ✅ Эффекты разрушения блоков
- ✅ Power-up блоки (золотые)
- ✅ Прогрессивная сложность

**Управление:**
- `A/D` или `Мышь` - Движение ракетки
- `SPACE` - Запуск мяча
- `ENTER` - Следующий уровень
- `R` - Перезапуск

**Компиляция:**
```bash
add_executable(Breakout Examples/BreakoutGame.cpp)
target_link_libraries(Breakout SAGE_Engine)

cmake --build build --config Release --target Breakout
.\build\bin\Release\Breakout.exe
```

**Изученные техники:**
- Физика отскока
- Grid-based level design
- Различные типы объектов (enum classes)
- Управление мышью
- Система уровней и прогрессии

---

## 🛠️ Другие примеры

### QuadTree Example
**Файл:** `QuadTreeExample.cpp`
Демонстрация пространственного разделения для оптимизации коллизий.

### Profiler Example
**Файл:** `ProfilerExample.cpp`
Использование встроенного профилировщика для измерения производительности.

### Particle Emitter Example
**Файл:** `ParticleEmitterExample.cpp`
Демонстрация различных эффектов частиц (огонь, дым, дождь, взрыв).

### Platformer Demo
**Файл:** `PlatformerDemo.cpp`
Платформер с физикой Box2D, прыжками и коллизиями.

---

## 🚀 Быстрый старт

### 1. Компиляция всех примеров

Добавьте в `CMakeLists.txt`:

```cmake
# Примеры игр
add_executable(SpaceShooter Examples/SpaceShooter.cpp)
target_link_libraries(SpaceShooter SAGE_Engine)

add_executable(FlappyBird Examples/FlappyBird.cpp)
target_link_libraries(FlappyBird SAGE_Engine)

add_executable(Breakout Examples/BreakoutGame.cpp)
target_link_libraries(Breakout SAGE_Engine)
```

### 2. Сборка

```bash
cmake --build build --config Release
```

### 3. Запуск

```bash
# Windows
.\build\bin\Release\SpaceShooter.exe
.\build\bin\Release\FlappyBird.exe
.\build\bin\Release\Breakout.exe
```

---

## 📖 Создание своей игры

### Шаблон минимальной игры:

```cpp
#include <SAGE/SAGE.h>
using namespace SAGE;

class MyGameScene : public Scene {
public:
    void OnSceneEnter() override {
        SAGE_INFO("Game started!");
        // Инициализация
    }
    
    void OnSceneUpdate(float deltaTime) override {
        // Игровая логика
    }
    
    void OnSceneRender() override {
        // Отрисовка
    }
};

class MyGame : public Game {
public:
    MyGame() : Game({.window = {.title = "My Game"}}) {}
    
    void OnGameInit() override {
        SceneManager::PushScene(std::make_shared<MyGameScene>());
    }
};

int main() {
    MyGame game;
    game.Run();
    return 0;
}
```

---

## 🎯 Рекомендуемый порядок изучения

1. **Flappy Bird** - Самая простая механика, базовая физика
2. **Breakout** - Коллизии, уровни, различные типы объектов
3. **Space Shooter** - Сложная игра с множеством систем
4. **Platformer Demo** - Box2D физика, продвинутые механики

---

## 📚 Дополнительные ресурсы

- 📖 [Руководство по разработке игр](../docs/GAME_DEVELOPMENT_GUIDE.md)
- 📖 [API документация](../docs/API.md)
- 🎮 [README движка](../README.md)

---

## 💡 Советы

### Оптимизация

```cpp
// ✅ ХОРОШО - используйте QuadTree для большого количества объектов
QuadTree<Enemy*> enemyTree(bounds, 10, 5);
for (auto& enemy : enemies) {
    enemyTree.Insert(enemy.GetBounds(), &enemy);
}
auto nearby = enemyTree.Query(player.GetBounds());

// ✅ ХОРОШО - профилируйте узкие места
void Update(float dt) {
    SAGE_PROFILE_FUNCTION();
    // ... код
}
```

### Управление ресурсами

```cpp
// ✅ ХОРОШО - загружайте ресурсы один раз
void OnSceneEnter() override {
    m_PlayerTexture = Texture::Create("player.png");
}

// ❌ ПЛОХО - загрузка каждый кадр
void OnSceneRender() override {
    auto tex = Texture::Create("player.png"); // Медленно!
}
```

### Организация кода

```cpp
// ✅ ХОРОШО - отдельные классы для сущностей
class Player {
    void Update(float dt);
    void Render();
    Rect GetBounds() const;
};

// ✅ ХОРОШО - используйте Scene для логических экранов
MenuScene → GameScene → PauseScene → GameOverScene
```

---

## 🤝 Вклад

Хотите добавить свой пример? Отлично!

1. Создайте файл `Examples/YourGame.cpp`
2. Следуйте структуре существующих примеров
3. Добавьте документацию в этот README
4. Создайте Pull Request

---

**Удачи в создании игр на SAGE Engine! 🚀**
