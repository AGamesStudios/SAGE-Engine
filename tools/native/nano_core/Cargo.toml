[package]
name = "nano_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "nano_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
once_cell = "1"
